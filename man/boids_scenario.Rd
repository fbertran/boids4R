% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenario.R
\name{boids_scenario}
\alias{boids_scenario}
\title{Generate and simulate a named boids scenario}
\usage{
boids_scenario(
  name = c("murmuration_3d", "predator_avoidance_2d", "obstacle_corridor_2d",
    "schooling_2d", "mixed_species_3d"),
  n = 500L,
  dimension = c("2d", "3d"),
  seed = NULL,
  steps = 120L,
  record_every = 2L
)
}
\arguments{
\item{name}{Scenario name.}

\item{n}{Number of boids.}

\item{dimension}{Scenario dimension. Some scenario names imply a dimension.}

\item{seed}{Optional seed.}

\item{steps}{Number of simulation steps.}

\item{record_every}{Record every \code{record_every} steps.}
}
\value{
A \code{boids_simulation} object.
}
\description{
Generate and simulate a named boids scenario
}
\examples{
sim <- boids_scenario(
  "schooling_2d",
  n = 20,
  steps = 5,
  record_every = 1,
  seed = 3
)
frames <- as.data.frame(sim)
table(frames$frame)

sim3d <- boids_scenario("murmuration_3d", n = 15, steps = 3, seed = 4)
range(as.data.frame(sim3d)$z)
}
