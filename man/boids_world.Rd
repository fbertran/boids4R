% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/world.R
\name{boids_world}
\alias{boids_world}
\title{Build a boids simulation world}
\usage{
boids_world(
  dimension = c("2d", "3d"),
  bounds = NULL,
  boundary = c("wrap", "reflect", "open"),
  obstacles = NULL,
  attractors = NULL,
  predators = NULL,
  species = NULL
)
}
\arguments{
\item{dimension}{World dimension, either \code{"2d"} or \code{"3d"}.}

\item{bounds}{Numeric matrix with rows \code{x}, \code{y}, and optionally \code{z}, and
columns \code{min} and \code{max}.}

\item{boundary}{Boundary behavior: \code{wrap}, \code{reflect}, or \code{open}.}

\item{obstacles, attractors, predators}{Data frames with coordinate columns.}

\item{species}{Optional species definition table.}
}
\value{
A \code{boids_world} list.
}
\description{
Build a boids simulation world
}
\examples{
bounds <- matrix(
  c(-2, -1, 2, 1),
  ncol = 2,
  dimnames = list(c("x", "y"), c("min", "max"))
)
world <- boids_world(
  "2d",
  bounds = bounds,
  boundary = "reflect",
  obstacles = data.frame(x = 0, y = 0, radius = 0.25),
  attractors = data.frame(x = 1.5, y = 0.4, strength = 0.5)
)
world$boundary
world$obstacles
}
